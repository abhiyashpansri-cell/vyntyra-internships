{
  "name": "vyntyra-internships-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Deployment wrapper for frontend + backend repo",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev"
  }
}
